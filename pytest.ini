[pytest]
testpaths = tests
asyncio_mode = auto

# tests/ has no __init__.py, so pytest puts that directory on sys.path rather
# than the repo root, and the modules under test are not importable. Invoking
# pytest as `python -m pytest` masks this by adding the CWD.
pythonpath = .
